{
  "name": "bricks",
  "version": "1.0.0",
  "description": "A game built with Electron and TypeScript",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "cp src/renderer/index.html dist/renderer/ && cp src/renderer/styles.css dist/renderer/ && mkdir -p dist/renderer/assets/fonts && cp -r src/renderer/assets/fonts/* dist/renderer/assets/fonts/",
    "watch": "tsc --watch",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron .",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "electron",
    "game",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.10.0",
    "electron": "^28.0.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.4",
    "typescript": "^5.3.0"
  }
}
